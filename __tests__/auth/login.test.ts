import MockDB from "../constants/mockDb"

describe("Login", () => {
    const mock = new MockDB()

    it.todo("should not except empty credentials")

    it.todo("should return a api key that is 18 characters long")

    it.todo("")
})
