export type TVault = {
    vault: [{
        id?: number,
        user_id: number,
        Vault_name: string,
        Vault_password: string,
        Vault_url: string,
        Vault_notes: string,
    }]
}